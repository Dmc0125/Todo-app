import Vue from 'vue';

export default new Vue();

// export const eventBus = new Vue();

// export class BusHelpers {
//   constructor() {
//     this.bus = eventBus;
//   }

//   emit(event) {
//     this.bus.$emit(event);
//   }

//   emitVal(event, val) {
//     this.bus.$emit(event, val);
//   }
// }
